---
#- hosts: "{{ filter }}:&dc_{{ dc }}:&cmp_nginx:&app_piwigo"
#  vars:
#    application_name: "piwigo"
#  become: true
#  roles:
#    - nginx

#- hosts: "{{ filter }}:&dc_{{ dc }}:&cmp_mariadb:&app_piwigo"
#  become: true
#  vars:
#    mysql_profile: "{{ mariadb_piwigo_profile }}"
#  roles:
#    - mysql

- hosts: "{{ filter }}:&dc_{{ dc }}:&cmp_php:&app_piwigo"
  become: true
  roles:
    - piwigo


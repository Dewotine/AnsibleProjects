{{ ansible_managed | comment }}
# prefork MPM
# StartServers: number of server processes to start
# MinSpareServers: minimum number of server processes which are kept spare
# MaxSpareServers: maximum number of server processes which are kept spare
# MaxRequestWorkers: maximum number of server processes allowed to start
# MaxConnectionsPerChild: maximum number of requests a server process serves

<IfModule mpm_worker_module>
	StartServers        {{ apache_prefork_server_limit }}
	MinSpareThreads     {{ apache_prefork_min_spare_servers }}
	MaxSpareThreads     {{ apache_prefork_max_spare_servers }}
	ServerLimit         {{ apache_prefork_server_limit }}
	MaxClients 	        {{ apache_prefork_max_clients }}
	MaxRequestsPerChild  {{ apache_prefork_max_requests_per_child }}
</IfModule>